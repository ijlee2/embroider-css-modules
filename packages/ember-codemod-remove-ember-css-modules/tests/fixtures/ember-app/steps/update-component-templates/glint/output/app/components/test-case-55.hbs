{{!--
  Dynamic expressions inside the {{local-class}} helper from
  ember-css-modules have limited support. The end-developer
  should refactor code.

  INCORRECT OUTPUT:

  Expected:
    class={{local this.styles "checkbox" (if this.isChecked "is-checked") (if (or @isDisabled @isReadOnly) "is-disabled")}}
--}}
<span
  class={{local-class
    (concat
      "checkbox"
      " "
      (if this.isChecked "is-checked")
      " "
      (if (or @isDisabled @isReadOnly) "is-disabled")
    )
  }}
>
  {{! Conditionally render a checkmark icon }}
</span>