{{! We can pass the local-class attribute to helpers. }}
<button
  aria-label="Cancel"
  class={{local this.styles "button" "button-secondary"}}
  type="button"
>
  {{svg-jar
    "clear"
    aria-hidden="true"
    class=(local this.styles (array "icon" "icon-secondary"))
  }}
</button>