{{!
  We can conditionally list local class names.
  Here, the else-clause is missing.
}}
<div
  class={{local-class this.styles (if (or this.someCondition1 this.someCondition2) (array "a1" "a2" "a3"))}}
>
  {{! Some content }}
</div>