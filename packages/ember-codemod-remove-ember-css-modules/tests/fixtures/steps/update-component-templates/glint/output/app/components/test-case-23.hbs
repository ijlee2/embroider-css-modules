{{!
  We can conditionally list local class names.
  Here, both unless- and else-clauses are present.
}}
<div
  class={{local this.styles (unless (or this.someCondition1 this.someCondition2) "a4" (array "a1" "a2" "a3"))}}
>
  {{! Some content }}
</div>