{{!
  We can pass the local-class attribute to components
  that we invoke with the curly-brace syntax.
}}
{{textarea
  disabled=isDisabled
  class=(local-class this.styles (concat "textarea" (if isDisabled " is-disabled")))
  value=value
}}