{{page-title "Products"}}

<Ui::Page @title="Products">
  <div
    class="{{if
        this.isPartOfNestProductDetailsExperiment
        (local this.styles "templates-products__shared-layout" "templates-products__products-with-details")
        (local this.styles "templates-products__shared-layout" "templates-products__products")
      }} {{this.styles.templates-products__sticky-container}} "
  >
    <div class={{this.styles.templates-products__filters}}>
      <div class={{this.styles.templates-products__filter}}>
        <Ui::Form::Input
          @data={{hash name=this.name}}
          @key="name"
          @label="Filter by"
          @onUpdate={{this.noOp}}
          @placeholder="Cake, pasta, etc."
        />
      </div>
    </div>

    <div class={{this.styles.templates-products__list}}>
      {{#each this.filteredProducts as |product|}}
        <Products::Product::Card @product={{product}} />
      {{else}}
        <p>
          No products found.
        </p>
      {{/each}}
    </div>

    <div class={{this.styles.templates-products__product-details}}>
      {{outlet}}
    </div>
  </div>
</Ui::Page>