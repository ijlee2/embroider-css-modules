{{!
  We can conditionally list local class names.
  Here, the else-clause is missing.
}}
<div
  local-class={{unless
    (or this.someCondition1 this.someCondition2)
    " a4 "
  }}
>
  {{! Some content }}
</div>