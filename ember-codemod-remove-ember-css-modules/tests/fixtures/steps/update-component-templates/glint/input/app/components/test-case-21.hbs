{{!--
  When we pass the class attribute to the {{component}} helper,
  the code should remain the same.
--}}
{{#let
  (component "some-component" class=" b1 ")
  as |SomeComponent|
}}
  <SomeComponent
    @someArgument="someValue"
  />
{{/let}}